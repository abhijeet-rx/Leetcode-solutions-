// Problem: 🌟 C++ || Professional Approach and In-Depth ExplanationIntuition
// Platform: geeksforgeeks
// Language: unknown
// Verdict: Accepted
// URL: https://www.geeksforgeeks.org/problems/tree-from-postorder-and-inorder/1
// Solved on: 2026-08-28T10:13:35.311Z

/* Structure of binary tree node
class Node {
    int data;
    Node left, right;
    Node(int x) {
        data = x;
        left = right = null;
    }
} */

class Solution {
    int index;
    int findpos(int[] inorder , int start , int end , int element){
        for(int i =start;i<=end;i++){
            if(inorder[i]==element){
                return i;
            }
        }
        return -1;
    }
    Node solve(int[] inorder, int[] postorder, int start , int end ,int n){
        if(start>end || index<0){
            return null;
        }
        int element  = postorder[index--];
        Node root = new Node(element);
        int position = findpos(inorder,start,end,element);
        root.right = solve(inorder,postorder,position+1,end,n);
        root.left = solve(inorder,postorder,start,position-1,n);
        return root;
    }
    public Node buildTree(int[] inorder, int[] postorder) {
        int n = postorder.length;
         index =n-1;
        return solve(inorder,postorder,0,n-1,n);
        
    }
}